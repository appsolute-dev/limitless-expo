import { Text, View } from "react-native";

export default function MacrosScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Macros Screen</Text>
    </View>
  );
}
